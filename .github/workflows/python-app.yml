# This workflow installs Python dependencies, runs tests, and deploys only from the main branch.

name: Python application

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Lint with flake8
        run: |
          # stop the build if there are Python syntax errors or undefined names
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Test with pytest
        run: pytest

  deploy:
    runs-on: ubuntu-latest        # ✅ fixed (was "run-on")
    needs: build                  # ✅ this waits until the build job completes
    if: github.ref == 'refs/heads/main'   # ✅ spacing fixed for condition

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4    # ✅ corrected indentation and order

      - name: Deploy to production
        run: |
          echo "Starting deployment..."
          # Add your real deployment commands here
